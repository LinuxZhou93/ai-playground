import React from "react";
import { ERPSidebar } from "@/components/erp/sidebar";
import { Bell, Moon, Sun } from "lucide-react";
import { ThemeToggle } from "./ThemeToggle";
import { CommandPalette } from "@/components/erp/command-palette";

export default function ERPLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <div className="flex bg-background min-h-screen overflow-hidden">
      {/* 侧边栏 */}
      <ERPSidebar />

      {/* 主内容区域 */}
      <div className="flex-1 flex flex-col min-w-0">
        {/* 顶部导航栏 — 毛玻璃效果 */}
        <header className="h-16 border-b flex items-center justify-between px-4 sm:px-8 bg-card/80 backdrop-blur-xl sticky top-0 z-10">
          {/* 左侧：Cmd+K 全局指令面板 */}
          <div className="flex items-center gap-4 flex-1 pl-10 md:pl-0">
            <CommandPalette />
          </div>

          {/* 右侧 */}
          <div className="flex items-center gap-3 sm:gap-6">
            <ThemeToggle />
            <button className="relative p-2 text-muted-foreground hover:text-foreground transition-colors">
              <Bell className="h-5 w-5" />
              <span className="absolute top-1.5 right-1.5 h-2 w-2 bg-red-500 rounded-full border-2 border-card animate-pulse" />
            </button>
            <div className="h-8 w-px bg-border hidden sm:block" />
            <div className="flex items-center gap-3">
              <div className="flex-col items-end hidden sm:flex">
                <span className="text-sm font-medium">Zhou Lin</span>
                <span className="text-xs text-muted-foreground">校长/Admin</span>
              </div>
              <div className="h-9 w-9 rounded-full bg-gradient-to-br from-emerald-500 to-teal-600 flex items-center justify-center text-white text-sm font-bold shadow-md shadow-emerald-500/20">
                Z
              </div>
            </div>
          </div>
        </header>

        {/* 内容区 */}
        <main className="flex-1 overflow-y-auto p-4 sm:p-8 bg-muted/10">
          {children}
        </main>
      </div>
    </div>
  );
}
