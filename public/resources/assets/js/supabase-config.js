const SUPABASE_CONFIG = {
    url: '',
    key: ''
};

// Expose to window immediately for other scripts
window.SUPABASE_CONFIG = SUPABASE_CONFIG;
console.log('✅ Supabase Config Shell Loaded (Credentials Removed)');
