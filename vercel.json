{
  "version": 2,
  "buildCommand": "cd openmaic-core && corepack enable && corepack prepare pnpm@10.28.0 --activate && pnpm install && pnpm run build",
  "outputDirectory": "openmaic-core/.next",
  "framework": "nextjs",
  "installCommand": "corepack enable && corepack prepare pnpm@10.28.0 --activate && pnpm install"
}
